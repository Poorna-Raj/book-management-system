spring:
  application:
    name: bms
  datasource:
    url: jdbc:mysql://localhost:3306/bms
    username: root
    password: codse242f-042
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always